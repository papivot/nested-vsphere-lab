---
# Credentials that must never be used for a left-behind appliance.
forbidden_passwords:
  - ""
  - "VMware1!"
  - "Passw0rd!"
  - "ChangeMe"
  - "ChangeMe-Strong-Passw0rd!"
  - "password"
  - "changeme"

# Reachability checks are best-effort warnings (UDP/ICMP may be filtered).
preflight_strict_reachability: false
