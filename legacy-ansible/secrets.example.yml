---
# Copy to secrets.yml and encrypt:  ansible-vault encrypt secrets.yml
# Then run:  ./run.sh --input input.yaml --stage 1 --ask-vault-pass
# The preflight role ABORTS if any of these are empty or left at a default/placeholder.

vault_registry_admin_password: "ChangeMe-Strong-Passw0rd!"
vault_ca_key_passphrase: ""          # optional; leave "" for an unencrypted CA key
